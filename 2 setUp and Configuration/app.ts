var a = 9;
function add(x: number, y: number): number {
    return x + y;
}

// for auto transpile run the command : tsc -w in terminal, it will watch for changes in the file and transpile it to JavaScript automatically.